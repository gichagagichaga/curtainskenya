<x-layouts::app :title="__('Testimonials')"><div class="mx-auto w-full max-w-7xl p-4 sm:p-6 lg:p-8"><div class="flex items-center justify-between border-b border-zinc-200 pb-6 dark:border-zinc-700"><div><p class="text-sm text-zinc-500">Curtains Kenya customer stories</p><h1 class="mt-1 text-2xl font-semibold text-zinc-900 dark:text-white">Testimonials</h1></div><a href="{{ route('admin.testimonials.create') }}" class="rounded-lg bg-zinc-900 px-4 py-2.5 text-sm font-medium text-white dark:bg-white dark:text-zinc-900">Add testimonial</a></div>@if(session('status'))<div class="mt-6 rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('status') }}</div>@endif<div class="mt-6 space-y-4">@forelse($testimonials as $testimonial)<article class="rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-700 dark:bg-zinc-900"><div class="flex flex-col gap-4 sm:flex-row sm:justify-between"><div><p class="font-semibold text-zinc-900 dark:text-white">{{ $testimonial->customer_name }} <span class="font-normal text-zinc-500">{{ $testimonial->location }}</span></p><p class="mt-2 text-amber-500">{{ str_repeat('★', $testimonial->rating) }}</p><p class="mt-3 text-sm leading-6 text-zinc-600 dark:text-zinc-300">{{ $testimonial->review }}</p></div><div class="shrink-0 text-right"><span class="rounded-full px-2.5 py-1 text-xs {{ $testimonial->is_published ? 'bg-emerald-100 text-emerald-800' : 'bg-zinc-100 text-zinc-600' }}">{{ $testimonial->is_published ? 'Published' : 'Hidden' }}</span><div class="mt-4 flex justify-end gap-3"><a href="{{ route('admin.testimonials.edit', $testimonial) }}" class="text-sm font-medium text-zinc-900 dark:text-white">Edit</a><form method="POST" action="{{ route('admin.testimonials.destroy', $testimonial) }}">@csrf @method('DELETE')<button class="text-sm font-medium text-red-600">Delete</button></form></div></div></div></article>@empty<div class="rounded-xl border border-zinc-200 bg-white p-12 text-center dark:border-zinc-700 dark:bg-zinc-900"><p class="text-zinc-500">No testimonials yet.</p></div>@endforelse</div>@if($testimonials->hasPages())<div class="mt-6">{{ $testimonials->links() }}</div>@endif</div></x-layouts::app>
