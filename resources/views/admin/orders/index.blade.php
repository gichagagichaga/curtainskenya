<x-layouts::app :title="__('Orders')">
    <div class="mx-auto w-full max-w-7xl p-4 sm:p-6 lg:p-8"><div class="flex flex-col gap-4 border-b border-zinc-200 pb-6 dark:border-zinc-700 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-sm font-medium text-zinc-500 dark:text-zinc-400">Curtains Kenya sales</p><h1 class="mt-1 text-2xl font-semibold tracking-tight text-zinc-900 dark:text-white">Orders</h1><p class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">Confirm, prepare, and complete customer orders.</p></div><a href="{{ route('dashboard') }}" class="text-sm font-medium text-zinc-600 hover:text-zinc-900 dark:text-zinc-300 dark:hover:text-white">← Dashboard</a></div>@if(session('status'))<div class="mt-6 rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800 dark:border-emerald-900 dark:bg-emerald-950/40 dark:text-emerald-300">{{ session('status') }}</div>@endif<div class="mt-6 space-y-4">@forelse($orders as $order)<article class="rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-700 dark:bg-zinc-900 sm:p-6"><div class="flex flex-col gap-5 lg:flex-row lg:justify-between"><div class="min-w-0"><div class="flex flex-wrap items-center gap-3"><h2 class="text-lg font-semibold text-zinc-900 dark:text-white">{{ $order->order_number }}</h2><span class="rounded-full bg-amber-100 px-2.5 py-1 text-xs font-medium text-amber-800 dark:bg-amber-950 dark:text-amber-300">{{ ucfirst($order->status) }}</span></div><p class="mt-2 text-sm text-zinc-500 dark:text-zinc-400"><span class="font-medium text-zinc-700 dark:text-zinc-200">{{ $order->customer_name }}</span> · {{ $order->customer_phone }} · {{ $order->customer_email }}</p><p class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">{{ $order->delivery_address }}, {{ $order->delivery_county }}</p><ul class="mt-4 space-y-2 text-sm text-zinc-700 dark:text-zinc-300">@foreach($order->items as $item)<li><span>{{ $item->product_name }} × {{ $item->quantity }} — KSh {{ number_format($item->line_total, 2) }}</span><span class="ml-2 rounded bg-zinc-100 px-2 py-0.5 font-mono text-xs text-zinc-600 dark:bg-zinc-800 dark:text-zinc-300">SKU: {{ $item->product_sku ?: 'Not recorded' }}</span></li>@endforeach</ul>@if($order->notes)<p class="mt-3 text-sm text-zinc-500 dark:text-zinc-400">Note: {{ $order->notes }}</p>@endif</div><div class="shrink-0 lg:w-48"><p class="text-lg font-semibold text-zinc-900 dark:text-white">KSh {{ number_format($order->total, 2) }}</p><p class="mt-1 text-xs text-zinc-400">{{ $order->created_at->format('j M Y, g:i a') }}</p><form method="POST" action="{{ route('admin.orders.update', $order) }}" class="mt-4">@csrf @method('PATCH')<label class="sr-only" for="status-{{ $order->id }}">Order status</label><select id="status-{{ $order->id }}" name="status" class="w-full rounded-lg border-zinc-300 text-sm dark:border-zinc-600 dark:bg-zinc-800">@foreach(['pending', 'confirmed', 'processing', 'completed', 'cancelled'] as $status)<option value="{{ $status }}" @selected($order->status === $status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="mt-2 w-full rounded-lg bg-zinc-900 px-3 py-2 text-sm font-medium text-white hover:bg-zinc-700 dark:bg-white dark:text-zinc-900">Save status</button></form></div></div></article>@empty<div class="rounded-xl border border-zinc-200 bg-white px-6 py-16 text-center dark:border-zinc-700 dark:bg-zinc-900"><h2 class="text-lg font-semibold text-zinc-900 dark:text-white">No orders yet</h2><p class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">Orders placed by customers will appear here.</p></div>@endforelse</div>@if($orders->hasPages())<div class="mt-6">{{ $orders->links() }}</div>@endif</div>
</x-layouts::app>
