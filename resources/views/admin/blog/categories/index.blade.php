<x-layouts::app :title="__('Blog categories')"><div class="mx-auto w-full max-w-6xl p-4 sm:p-6 lg:p-8"><div class="flex items-center justify-between border-b border-zinc-200 pb-6 dark:border-zinc-700"><div><p class="text-sm text-zinc-500">Curtains Kenya Journal</p><h1 class="mt-1 text-2xl font-semibold dark:text-white">Blog categories</h1></div><a href="{{ route('admin.blog.categories.create') }}" class="rounded-lg bg-zinc-900 px-4 py-2.5 text-sm font-medium text-white dark:bg-white dark:text-zinc-900">Add category</a></div>@if(session('status'))<p class="mt-5 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">{{ session('status') }}</p>@endif<div class="mt-6 overflow-hidden rounded-xl border border-zinc-200 bg-white dark:border-zinc-700 dark:bg-zinc-900"><table class="min-w-full divide-y divide-zinc-200 text-left text-sm dark:divide-zinc-700"><thead class="bg-zinc-50 text-xs text-zinc-500 uppercase dark:bg-zinc-800"><tr><th class="px-5 py-3">Category</th><th class="px-4 py-3">Articles</th><th class="px-4 py-3">Visibility</th><th class="px-5 py-3 text-right">Action</th></tr></thead><tbody class="divide-y divide-zinc-100 dark:divide-zinc-800">@foreach($categories as $blogCategory)<tr><td class="px-5 py-4"><p class="font-medium dark:text-white">{{ $blogCategory->name }}</p><p class="text-xs text-zinc-500">{{ $blogCategory->slug }}</p></td><td class="px-4 py-4">{{ $blogCategory->posts_count }}</td><td class="px-4 py-4">{{ $blogCategory->is_active ? 'Visible' : 'Hidden' }}</td><td class="px-5 py-4 text-right"><a href="{{ route('admin.blog.categories.edit', $blogCategory) }}" class="font-medium hover:underline dark:text-white">Edit</a></td></tr>@endforeach</tbody></table>@if($categories->hasPages())<div class="border-t p-4">{{ $categories->links() }}</div>@endif</div></div></x-layouts::app>
