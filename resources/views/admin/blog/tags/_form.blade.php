<form method="POST" action="{{ $tag ? route('admin.blog.tags.update', $tag) : route('admin.blog.tags.store') }}" class="mt-6 space-y-6">@csrf @if($tag) @method('PUT') @endif<div class="space-y-5 rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-700 dark:bg-zinc-900"><div><label class="text-sm font-medium dark:text-white">Name</label><input name="name" required value="{{ old('name', $tag?->name) }}" class="mt-2 w-full rounded-lg border-zinc-300 dark:border-zinc-700 dark:bg-zinc-800">@error('name')<p class="mt-2 text-sm text-red-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-medium dark:text-white">URL slug</label><input name="slug" value="{{ old('slug', $tag?->slug) }}" class="mt-2 w-full rounded-lg border-zinc-300 dark:border-zinc-700 dark:bg-zinc-800"></div><label class="flex gap-2 text-sm dark:text-white"><input name="noindex" value="1" type="checkbox" @checked(old('noindex', $tag?->noindex))> Prevent search indexing</label></div><div class="flex justify-between"><a href="{{ route('admin.blog.tags.index') }}" class="rounded-lg px-4 py-2 text-sm">Cancel</a><div class="flex gap-3">@if($tag)<button form="delete-tag" class="px-4 py-2 text-sm text-red-700">Delete</button>@endif<button class="rounded-lg bg-zinc-900 px-5 py-2.5 text-sm text-white dark:bg-white dark:text-zinc-900">Save tag</button></div></div></form>@if($tag)<form id="delete-tag" method="POST" action="{{ route('admin.blog.tags.destroy', $tag) }}">@csrf @method('DELETE')</form>@endif
