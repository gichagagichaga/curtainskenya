<x-layouts::app :title="__('Blog articles')"><div class="mx-auto w-full max-w-7xl p-4 sm:p-6 lg:p-8"><div class="flex items-center justify-between border-b border-zinc-200 pb-6 dark:border-zinc-700"><div><p class="text-sm text-zinc-500">Curtains Kenya Journal</p><h1 class="mt-1 text-2xl font-semibold dark:text-white">Blog articles</h1></div><a href="{{ route('admin.blog.posts.create') }}" class="rounded-lg bg-zinc-900 px-4 py-2.5 text-sm font-medium text-white dark:bg-white dark:text-zinc-900">Add article</a></div>@if(session('status'))<p class="mt-5 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">{{ session('status') }}</p>@endif<div class="mt-6 overflow-hidden rounded-xl border border-zinc-200 bg-white dark:border-zinc-700 dark:bg-zinc-900">@if($posts->isNotEmpty())<div class="overflow-x-auto"><table class="min-w-full divide-y divide-zinc-200 text-left text-sm dark:divide-zinc-700"><thead class="bg-zinc-50 text-xs text-zinc-500 uppercase dark:bg-zinc-800"><tr><th class="px-5 py-3">Article</th><th class="px-4 py-3">Category</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Published</th><th class="px-5 py-3 text-right">Action</th></tr></thead><tbody class="divide-y divide-zinc-100 dark:divide-zinc-800">@foreach($posts as $post)<tr><td class="px-5 py-4"><p class="font-medium text-zinc-900 dark:text-white">{{ $post->title }}</p><p class="mt-1 text-xs text-zinc-500">{{ $post->author->name }}</p></td><td class="px-4 py-4">{{ $post->category?->name ?? '—' }}</td><td class="px-4 py-4"><span class="rounded-full px-2 py-1 text-xs {{ $post->status === 'published' ? 'bg-emerald-100 text-emerald-800' : 'bg-zinc-100 text-zinc-700' }}">{{ ucfirst($post->status) }}</span></td><td class="px-4 py-4">{{ $post->published_at?->format('j M Y') ?? '—' }}</td><td class="px-5 py-4 text-right"><a class="font-medium hover:underline dark:text-white" href="{{ route('admin.blog.posts.edit', $post) }}">Edit</a></td></tr>@endforeach</tbody></table></div>@if($posts->hasPages())<div class="border-t p-4">{{ $posts->links() }}</div>@endif @else <p class="p-10 text-center text-zinc-500">Your first helpful guide starts here.</p>@endif</div></div></x-layouts::app>
